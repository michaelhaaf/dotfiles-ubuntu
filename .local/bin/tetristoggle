#!/bin/bash
device="$(v4l2-ctl --list-devices | grep -A1 'AV TO USB' | sed 's/^[[:space:]]//' | tail -n 1)"
pkill -f "$device" || mpv --geometry=+0+0 --autofit=50% --demuxer-lavf-format=video4linux2 --demuxer-lavf-o-set=input_format=mjpeg av://v4l2:"$device" --profile=low-latency
